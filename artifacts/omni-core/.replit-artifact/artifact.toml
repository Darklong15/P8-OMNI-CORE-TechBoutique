kind = "web"
previewPath = "/"
title = "OMNI CORE"
version = "1.0.0"
id = "artifacts/omni-core"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24723

[services.development]
run = "pnpm --filter @workspace/omni-core run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/omni-core", "run", "build" ]
publicDir = "artifacts/omni-core/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24723"
BASE_PATH = "/"
